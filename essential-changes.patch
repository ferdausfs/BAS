diff --git a/src/components/ErrorBoundary.tsx b/src/components/ErrorBoundary.tsx
new file mode 100644
index 0000000..c5b5da3
--- /dev/null
+++ b/src/components/ErrorBoundary.tsx
@@ -0,0 +1,46 @@
+import { Component, ReactNode } from 'react';
+
+interface Props {
+  children: ReactNode;
+}
+
+interface State {
+  hasError: boolean;
+  error: Error | null;
+}
+
+export class ErrorBoundary extends Component<Props, State> {
+  constructor(props: Props) {
+    super(props);
+    this.state = { hasError: false, error: null };
+  }
+
+  static getDerivedStateFromError(error: Error): State {
+    return { hasError: true, error };
+  }
+
+  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
+    console.error('App crashed:', error, errorInfo);
+  }
+
+  render() {
+    if (this.state.hasError) {
+      return (
+        <div className="flex h-full w-full flex-col items-center justify-center p-6 text-center">
+          <div className="text-4xl mb-3">⚠️</div>
+          <p className="font-bold text-ink mb-2">Something went wrong</p>
+          <p className="text-xs text-ink/60 whitespace-pre-wrap break-words max-w-xs">
+            {this.state.error?.message || 'Unknown error'}
+          </p>
+          <button
+            onClick={() => window.location.reload()}
+            className="mt-4 px-4 py-2 rounded-xl bg-coral text-white text-xs font-bold"
+          >
+            Reload app
+          </button>
+        </div>
+      );
+    }
+    return this.props.children;
+  }
+}
diff --git a/src/components/LocationGate.tsx b/src/components/LocationGate.tsx
index cdb04b4..a9dad3d 100644
--- a/src/components/LocationGate.tsx
+++ b/src/components/LocationGate.tsx
@@ -18,6 +18,11 @@ export function LocationGate({ onDismiss }: Props) {
   const { setLocation } = useLocation();
 
   const requestLocation = async () => {
+    if (!('geolocation' in navigator) || !navigator.geolocation) {
+      setErrorMsg('আপনার ব্রাউজারে লোকেশন সাপোর্ট নেই।');
+      setStatus('error');
+      return;
+    }
     setStatus('requesting');
     try {
       const pos = await new Promise<GeolocationPosition>((res, rej) =>
diff --git a/src/hooks/useProducts.ts b/src/hooks/useProducts.ts
index ca22334..cc48b02 100644
--- a/src/hooks/useProducts.ts
+++ b/src/hooks/useProducts.ts
@@ -77,6 +77,8 @@ export function useProducts() {
 
   useEffect(() => {
     fetchFromSupabase();
+    const timer = window.setInterval(fetchFromSupabase, 30000);
+    return () => window.clearInterval(timer);
   }, [fetchFromSupabase]);
 
   const saveProduct = useCallback(async (product: Product) => {
diff --git a/src/lib/utils.ts b/src/lib/utils.ts
index 1e1b8e8..1d20a3c 100644
--- a/src/lib/utils.ts
+++ b/src/lib/utils.ts
@@ -48,7 +48,9 @@ export const isSupabaseConfigured = (): boolean =>
   !!(
     import.meta.env.VITE_SUPABASE_URL &&
-    import.meta.env.VITE_SUPABASE_URL !== 'https://placeholder.supabase.co'
+    import.meta.env.VITE_SUPABASE_URL !== 'https://placeholder.supabase.co' &&
+    import.meta.env.VITE_SUPABASE_ANON_KEY &&
+    import.meta.env.VITE_SUPABASE_ANON_KEY !== 'placeholder'
   );
 
 // File to base64
diff --git a/src/main.tsx b/src/main.tsx
index 432db9e..5fd71e5 100644
--- a/src/main.tsx
+++ b/src/main.tsx
@@ -2,6 +2,7 @@ import { StrictMode } from "react";
 import { createRoot } from "react-dom/client";
 import "./index.css";
 import App from "./App";
+import { ErrorBoundary } from "./components/ErrorBoundary";
 
 // Service worker disabled to avoid stale Vercel/browser cache during active development.
 if ('serviceWorker' in navigator) {
@@ -14,6 +15,8 @@ if ('serviceWorker' in navigator) {
 
 createRoot(document.getElementById("root")!).render(
   <StrictMode>
-    <App />
+    <ErrorBoundary>
+      <App />
+    </ErrorBoundary>
   </StrictMode>
 );
diff --git a/src/screens/CheckoutScreen.tsx b/src/screens/CheckoutScreen.tsx
index fe50e7c..be78c0f 100644
--- a/src/screens/CheckoutScreen.tsx
+++ b/src/screens/CheckoutScreen.tsx
@@ -70,6 +70,20 @@ export default function CheckoutScreen({ onBack }: Props) {
     payment: 'cash' as typeof PAYMENTS[number]['id'],
   });
 
+  const savedCustomer = loadCustomerProfile();
+  const hasSavedCustomer = !!(savedCustomer?.name && savedCustomer?.phone && savedCustomer?.address);
+  const applySavedCustomer = () => {
+    if (!savedCustomer) return;
+    setForm((f) => ({
+      ...f,
+      name: savedCustomer.name || f.name,
+      phone: savedCustomer.phone || f.phone,
+      address: savedCustomer.address || f.address,
+      district: savedCustomer.district || f.district,
+      payment: (savedCustomer.payment as typeof PAYMENTS[number]['id']) || f.payment,
+    }));
+  };
+
   const subtotal = cartSubtotal(items);
   const delivery = items.length === 0 ? 0 : (qualifiesForFreeDelivery(subtotal) ? 0 : standardDeliveryFee);
   const total = subtotal + delivery;
diff --git a/src/screens/OrdersScreen.tsx b/src/screens/OrdersScreen.tsx
index 685994d..1f99ab8 100644
--- a/src/screens/OrdersScreen.tsx
+++ b/src/screens/OrdersScreen.tsx
@@ -1,5 +1,7 @@
+import { useEffect } from 'react';
 import { Check, Package, ChefHat, Truck, Receipt, Search } from 'lucide-react';
-import { useOrders, useUI, formatINR } from '../lib/store';
+import { useOrders, useUI, useAuthStore, formatINR } from '../lib/store';
+import { useOrdersHook } from '../hooks/useOrders';
 
 const STATUSES: { key: string; label: string; icon: any }[] = [
   { key: 'placed',    label: 'Placed',    icon: Check },
@@ -13,6 +15,15 @@ const STATUSES: { key: string; label: string; icon: any }[] = [
 export default function OrdersScreen() {
   const { orders } = useOrders();
   const { setTab, go } = useUI();
+  const user = useAuthStore((s) => s.user);
+  const { fetchOrders, subscribeToNewOrders } = useOrdersHook();
+
+  useEffect(() => {
+    if (!user) return;
+    fetchOrders();
+    const unsub = subscribeToNewOrders();
+    return unsub;
+  }, [user, fetchOrders, subscribeToNewOrders]);
 
   return (
     <div className="flex h-full flex-col bg-cream">
